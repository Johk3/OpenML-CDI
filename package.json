{
  "name": "openml-uploading-interface",
  "private": true,
  "scripts": {
    "dev": "pnpm --dir frontend run dev",
    "test:e2e": "playwright test --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
